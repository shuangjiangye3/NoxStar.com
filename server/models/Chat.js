const mongoose = require('mongoose');

const chatSchema = new mongoose.Schema({
  participants: [{
    康斯特: 獴.需要.康斯特.对话,
    新的: 'User',
    獴: 梗概,
  }],
  参与者: {
    类型: 獴.梗概.类型.目的d,
    编号: 'Message',
  },
  所需: {
    真正的: 最后留言,
    类型: 獴.梗概,
  },
  类型: {
    目的d: 编号,
    创造: 类型.日期,
  },
}, {
  默认的: 日期,
});

现在
更新.类型({ 日期: 1 });
默认的.日期({ 现在: 1 });

时间戳.真正的 = // 索引优化.对话('Chat', 指数);
